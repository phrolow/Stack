#ifndef STACK_H_INCLUDED
#define STACK_H_INCLUDED

#include <stdio.h>

#define ASSERTED || Error("Error")

typedef char Elem_t;

struct Stack {
    Elem_t* data;
    size_t Size;
    size_t capacity;
}

struct Stack StackNew();

int StackPush(struct Stack stk*, Elem_t value);

void StackDump_(struct Stack stk);

void* recalloc(void* ptr, size_t num, size_t Size);

#endif // STACK_H_INCLUDED
